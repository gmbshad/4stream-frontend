import alt from '../alt';

class DashboardActions {

  handleAuthStoreUpdate(state) {
    return state;
  }

  handleSettingsStoreUpdate(state) {
    return state;
  }
}

export default alt.createActions(DashboardActions);
